<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.VIRTUAL_INPUT_DEVICE" />
    <uses-permission android:name="android.permission.INJECT_EVENTS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- Vendored AimSvc (embedded Shizuku) server-side permission; checked by the
         adb-uid service process when a client attaches. Signature-level so only this
         APK can satisfy it. -->
    <permission
        android:name="team.maodie.aimbot.permission.AIMBOT_API"
        android:protectionLevel="signature" />
    <uses-permission android:name="team.maodie.aimbot.permission.AIMBOT_API" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aimbot"
        android:extractNativeLibs="true"
        tools:targetApi="31">

        <activity
            android:name=".ui.MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.SettingsActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:enableOnBackInvokedCallback="true"
            android:exported="false" />

        <!-- Vendored AimSvc provider. The AimSvc server (running under adb shell)
             delivers its binder into this provider via call("sendBinder", ...).
             The provider authority is .aimbot_svc (not .shizuku) so external apps
             cannot enumerate it. -->
        <provider
            android:name="team.maodie.aimbot.svc.client.AimSvcProvider"
            android:authorities="${applicationId}.aimbot_svc"
            android:directBootAware="true"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <service
            android:name=".service.FloatService"
            android:foregroundServiceType="mediaProjection"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:enabled="true"
            android:exported="false" />

        <!-- Foreground service that runs mDNS discovery + SPAKE2 pairing flow for
             the embedded AimSvc. Started from Settings UI. -->
        <service
            android:name="team.maodie.aimbot.svc.PairingForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="adb_pairing" />
        </service>

        <!-- Qualcomm QNN HTP DSP -->
        <uses-native-library android:name="libcdsprpc.so" android:required="false" />
        <uses-native-library android:name="libadsprpc.so" android:required="false" />
        <uses-native-library android:name="libvmmem.so" android:required="false" />
        <uses-native-library android:name="vendor.qti.hardware.dsp@1.0.so" android:required="false" />
        <uses-native-library android:name="libOpenCL.so" android:required="false" />
    </application>

</manifest>